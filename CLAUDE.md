# jigkit 저장소에서 작업할 때

- 근거에는 등급을 붙인다 (`[M]` 실측 / `[D]` 문서 / `[J]` 판단 / `[?]` 불확실).
  **`[?]` 를 남기지 말고 잰다** — 확인 방법을 적을 수 있으면 대개 그 자리에서 돌릴 수 있다.
- 동작을 가정하지 말고 `probe/` 로 확인하고 결과를 `probe/results/` 에 남긴다.
- 그 자리에서 못 재는 것은 `probe/PENDING.md` 에 **확인 방법과 함께** 등록한다.
  세션 시작 훅이 보여주므로 외울 필요 없다 — 재고 나면 결과를 남기고 항목을 지운다.
- 컴파일러를 건드렸으면 `jig golden`, 프로필을 건드렸으면 `jig doctor`,
  게이트·라우팅을 건드렸으면 `jig selftest`. **게이트는 false negative 가 위험하다** —
  안 뜨면 아무 일도 없는 것처럼 보이므로 회귀 검사 없이 고치지 않는다.
- 명령을 추가·변경하면 `adapters/commands.py` 의 표만 고치고 `jig docs --update`.
  도움말과 README 명령 블록은 **손으로 쓰지 않는다** — 생성물이다.
- 문서는 `jig touched` 가 짚어 준다. 커밋 시점에 훅이 자동으로 보여주므로 따로 외울
  필요는 없다. 다만 그 목록은 **"봐야 할 곳"이지 "틀린 곳"이 아니다** — 판단은 직접 한다.
- 영문 `README.md` 가 정본이고 `README.ko.md` 는 번역이다. 절 구조를 1:1 로 맞춘다.
