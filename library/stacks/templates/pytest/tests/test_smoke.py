"""스모크 테스트. 이 한 개가 있어야 갓 만든 프로젝트에서도 `pytest` 가 신호를 준다.

테스트가 0개면 pytest 는 "수집 실패" 로 나가고, verify 가 통과인지 실패인지가
프로젝트 상태와 무관해진다 — 첫 커밋부터 검사가 죽는다. 실제 테스트가 생기면 지운다.
"""


def test_smoke() -> None:
    assert True
